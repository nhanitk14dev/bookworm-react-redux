/*
    https://v5.reactrouter.com/web/example/route-config
    !important: Routes work different in v6. Currently, we use routes v6 https://reactrouter.com/en/main

    // A special wrapper for <Route> that knows how to
    // handle "sub"-routes by passing them in a `routes`
    // prop to the component it renders.
*/
// import { RouteComponentProps, LinkProps, Route } from 'react-router-dom';

// interface RouteProps extends LinkProps {
//     path: string;
//     routes: string;
// }

const RouteWithSubRoutes = () => {
    return (
        <></>
    );
}

export default RouteWithSubRoutes;